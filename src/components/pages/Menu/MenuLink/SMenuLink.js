import styled from "styled-components";
import { NavLink } from "react-router-dom";

import { tMenuLink } from "style/typo";

export const SMenuLink = styled(NavLink)`
  ${tMenuLink}
  color: white;
`;
